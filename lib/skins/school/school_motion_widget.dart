import 'package:flutter/material.dart';

class SchoolMotionWidget extends StatelessWidget {
  const SchoolMotionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
